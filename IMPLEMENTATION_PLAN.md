# Genesis Agents — Exhaustive Validation Implementation Plan

Source requirement: `CatoGensisAgentBuild.md` (also committed in the Cato repo — this is a joint
Cato+Genesis program). Executed via `continuous-build-verify` in LOOP-ONLY mode; one task below =
one `ralph-wiggum-loop` build + one `truth-fix-loop` audit before advancing.

Ground truth for every task below comes from a live inventory pass (2026-08-04): a real
`GET /agents` call against `https://swarmsync-agents.onrender.com` returned all 57 live slugs;
`skill_bundles/*.json` (24 files) were read individually; `runtime/tool_policy.py`,
`bundle_loader.py`, `eval/genesis_client.py`, and `eval/datasets/*.jsonl` were cross-referenced
against them. "100% tested" means every row in the resulting agent/tool/capability matrix has a
test and a verdict — not that every future defect is impossible.

**Non-negotiables carried into every task:**
- No agent may return apparent success when no tool ran, the tool failed, the operation is
  pending, the result is unverified, a fallback persona answered instead, or evidence is absent.
- `testContext`/`live_test`/persona-only results are labeled separately and never counted as
  full-agent (AgentRuntime + real tool dispatch) verification.
- A capability that cannot be made safe or reliable gets disabled and removed from the advertised
  capability inventory — not silently left broken.
- Guarded finance/deploy agents (`genesis_deploy_x402`, `genesis_finance_x402`,
  `genesis_billing_x402`, `genesis_commerce_x402`) are never invoked in real/full mode by any task
  below — mocked/guard-proof only.

---

## Phase A — Structural fixes surfaced by the inventory (fix before building more tests on top)

- [x] **[SUPERSEDES the original naive "just re-key TOOL_RISK" framing of this task — see below]** `docs/FINANCE-TOOL-CONTRACTS.md` is an existing, authoritative, staged remediation plan (Section 7) for exactly the `TOOL_RISK` compound-name mismatch, and it explicitly names "map registered names by category prefix, or add them to `TOOL_RISK` in one commit" as **the forbidden fix** — because several of the tools that would newly become dispatchable (the twelve `*_get_budget_metrics`/`*_get_audit_log`/`*_get_alerts` tools, `domain_get_cost_summary`) currently return fabricated/invented constants as if real, and one bare-word mapping (`"pricing" → RISK_READ_ONLY`) would make a money-spending tool (`pricing_purchase_dataset`) callable by every agent. Implement **only Section 7 Phase 1** as this task: add `TOOL_RISK_BY_NAME` keyed on registered names (hand-written, no prefixes, no defaults), `get_tool_risk_by_name()`, and shadow-mode logging (`tool.policy.shadow` event with `legacy_risk`/`new_risk`/`would_allow_legacy`/`would_allow_new`) wired into the dispatcher **without** changing enforcement — `check_tool_policy` keeps using the legacy `TOOL_RISK` path. Add `test_tool_risk_coverage.py` asserting every name in the live tool registry appears in `TOOL_RISK_BY_NAME` with none falling through to a default. Do not touch `SLUG_ALLOWED_RISKS` in this task.
- [x] Implement `docs/FINANCE-TOOL-CONTRACTS.md` Section 7 Phase 3: first, adopt the Section 5 truthful-failure envelope (returns `provider_unconfigured` rather than a fabricated success — no invented constants) for every tool Appendix A classifies `READ_ONLY` (the twelve `*_get_budget_metrics`/`*_get_audit_log`/`*_get_alerts` tools, `domain_get_cost_summary`, `finance_generate_finance_report`, `billing_generate_revops_report`, `domain_check_availability`, `data_bigquery_query`) — this is Section 7's explicit precondition and must land first. Then, in one commit: switch `check_tool_policy` to `TOOL_RISK_BY_NAME`, remove `RISK_PAYMENT` from `genesis-finance`'s allowed set (leaving `genesis-billing`/`genesis-commerce`/`genesis-pricing` on `DEFAULT_ALLOWED_RISKS`), and add the exhaustive `test_tool_policy_matrix.py` gating test with a checked-in `expected_policy_matrix.json` fixture covering every `(registered_name, slug)` pair — asserting `RISK_PAYMENT` appears in no slug's allowed set and every `PROHIBITED_TOOLS` name is `ok: false` for every slug. Also fix `main.py`'s fail-open gateway auth (returns early with only a warning when `GATEWAY_API_KEY` is unset) into a hard startup failure, per Section 7's stated related precondition, with a regression test.
- [ ] Resolve the 4 orphaned skill bundles (`genesis-maintenance.json`, `genesis-onboarding.json`, `genesis-domain.json`, `genesis-pricing.json`): none of their live-slug counterparts (`maintenance_agent`, `onboarding-automation`, `domain_name_agent`, `pricing_agent`) resolve through `bundle_loader.BUNDLE_SLUG_ALIASES` (`bundle_loader.py:14-25`) or the hyphen/underscore fallback (`bundle_loader.py:28-37`). For each, either add the correct alias so the bundle actually loads, or formally document it as retired/unreachable-by-design and remove its `tools_advertised` claims. Add a regression test enumerating every `skill_bundles/*.json` file and asserting it resolves from at least one live slug, or is explicitly listed in a `KNOWN_UNREACHABLE_BUNDLES` allowlist with a reason.
- [ ] Resolve the `onboarding_agent` / `genesis-onboarding.json` collision: the live slug `onboarding_agent` is currently aliased to the HR/ATS bundle (`genesis-hr.json`), while the distinct product/UX-onboarding bundle (`genesis-onboarding.json`) sits unused. Decide the correct mapping (rename one of the two live-facing concepts, or split into two distinct slugs) and fix the alias table accordingly, with a regression test pinning the corrected mapping.
- [ ] Add server-side deployment-domain enforcement for `genesis_deploy_x402`: confirm via a direct (non-full-mode) probe that it currently returns a normal HTTP 200 persona answer with no server-side block on real deploy actions — the only existing guard is client-side, inside `eval/genesis_client.py`'s `MONEY_DOMAIN_MARKERS`. Add a real server-side guard in `main.py` (mirroring the containment already correct in Cato's `genesis.py::MONEY_DOMAIN_AGENTS`) so this agent refuses unsafe real-mode deploy calls at the gateway itself. Add a regression test calling it directly (bypassing the eval client) asserting a safe refusal/guard response. (`genesis_finance_x402`/`genesis_billing_x402`/`genesis_commerce_x402` are excluded from this task — their server-side enforcement is fully governed by `docs/FINANCE-TOOL-CONTRACTS.md` Section 7, tasks above; do not duplicate a second guard mechanism for the same tools.)
- [ ] Remove the duplicate tool registrations for `vercel_deploy`, `netlify_deploy`, and `github_tool` (each is registered twice — once from a dedicated `tools/*_tool.py` module, once again from inside `tools/deploy_tool.py`). Keep one registration per tool, delete the dead duplicate, add a regression test asserting `tools/__init__.py`'s auto-discovery registers each tool name exactly once.
- [ ] Fix the marketing-agent fabrication defect found in the prior evaluation run (`genesis_marketing_x402` claimed to have fetched/OCR'd/analyzed competitor material it never actually retrieved). Correct the system prompt/tool-use logic so the agent refuses to claim retrieval, OCR, or analysis of material it did not actually fetch, and add a permanent regression test (`llm_only-NEG-marketing-live-competitor-data` plus at least one new adversarial variant) proving this failure mode cannot recur.

## Phase B — Test-harness infrastructure (needed before full-matrix execution)

- [ ] Build `docs/validation/agent-inventory.json`: a generated (not hand-written) machine-readable manifest enumerating all 57 live slugs and all 24 skill bundles, each with slug, purpose, declared capabilities, allowed tools, prohibited tools, risk classification (safe / guarded-finance / guarded-deploy / other), live-testable Y/N, mock-required Y/N, and current eval-coverage status. Generation script must call the live `GET /agents` endpoint and read `skill_bundles/*.json` directly rather than hardcoding the list, so it stays correct as bundles change.
- [ ] Extend the eval harness with async job-polling support: `genesis_builder_x402`, `genesis_research_x402`, `genesis_deploy_x402`, `genesis_qa_x402`, and `genesis_meta_x402` all use `job_mode: "async"` and currently only the initial `job_id`/`poll_url` envelope is captured, never the actual completed result. Add `GET /agents/jobs/{job_id}` polling (with a sane timeout and backoff) to the target adapter so these five agents' real output can be scored, not just their envelope.
- [ ] Build a deterministic-evaluators module (`eval/deterministic.py` or similar) covering every category from the required test matrix that can be checked mechanically rather than by LLM judge: structured-output/schema compliance, correct tool selection given the request, refusal-to-fabricate-tool-results, generic-fallback prevention (the request used a slug that IS in `AGENT_PERSONAS`/`skill_bundles` and got a matching, non-fabricated persona back — not the `main.py:1690-1695` fallback), cross-agent routing correctness, cost/latency-limit enforcement, retry/idempotency behavior on duplicate requests. Each evaluator returns a pass/fail plus the concrete evidence it checked, not a vibe.
- [ ] Build `docs/validation/capability-test-matrix.json`: a generated cross-reference of every agent (from Phase B's inventory) against every applicable category from the 30-category test matrix in `CatoGensisAgentBuild.md` Part 2, with current status (not yet run / pass / fail / guarded / mocked) per cell. This becomes the tracking artifact Phase C fills in.

## Phase C — Full-matrix test execution (grouped by risk tier, not one task per agent, to keep this tractable — but every agent must appear in exactly one group's results)

- [ ] Generate and run the full applicable test suite (all 30 categories from `CatoGensisAgentBuild.md` Part 2 that apply to a tool-capable agent) for the 20 skill-bundle-backed agents, through the real `AgentRuntime`/tool-dispatch path wherever safe (not `testContext`/`live_test` bypass) — 3 repetitions per nondeterministic scenario, 5 repetitions for any agent touching finance/compliance/security. Record results into `capability-test-matrix.json` and `test-results.json`; any category that cannot safely run live must be explicitly labeled mocked/guarded, never silently skipped.
- [ ] Generate and run the applicable subset of the 30-category matrix (persona/domain behavior, instruction-following, factual grounding, unknown-information handling, ambiguous/contradictory requests, structured-output compliance, long-context behavior, prompt injection, indirect prompt injection, out-of-authority requests, secret-exfiltration attempts — the categories that make sense for a persona with zero tools) for all 36 persona-only agents (`AGENT_PERSONAS` entries with no matching skill bundle), 3 repetitions each. Record into the same matrix/results files.
- [ ] Generate and run guarded-path-only tests (authorization/denial/sandbox/mocked execution paths — never real financial, payment, or deployment actions) for `genesis_deploy_x402`, `genesis_finance_x402`, `genesis_billing_x402`, and `genesis_commerce_x402`, 5 repetitions each, proving each one correctly refuses or sandboxes every dangerous real-mode request while still answering safely in guarded/mock mode.
- [ ] Run the security/injection/exfiltration/authority-boundary categories (prompt injection, indirect prompt injection, requests outside the agent's authority, secret-exfiltration attempts) as a dedicated cross-cutting pass across every reachable agent (all 57), since these apply uniformly regardless of tool access — a persona-only agent can still leak a system prompt or comply with an out-of-scope request even with no tools to misuse.

## Phase D — Tool-level suites (Part 3 of the build spec)

- [ ] Build and run the full tool-level suite (schema validation, auth, authz, timeouts, retries, error propagation, rate limits, idempotency, duplicate protection, read-back verification, secret redaction, logging, network failures, partial responses, false-success prevention) for the 9 tools that are currently actually dispatchable (`file_write`, `code_format`, `genesis_call`, `conduit`, `workspace_shell`, `web_search`, `web_fetch`, `vercel_deploy`, `netlify_deploy`) — real integrations for the safe ones, mocks/fixtures for `vercel_deploy`/`netlify_deploy`/`workspace_shell`/`genesis_call` (recursion budget). Every mocked test result must be labeled mocked, never presented as live-integration proof.
- [ ] For the registered-but-never-dispatchable tools outside the finance-adjacent set already governed by `docs/FINANCE-TOOL-CONTRACTS.md` (Phase A tasks above cover `finance_*`/`billing_*`/`commerce_*`/`pricing_*`/`domain_*`/`data_*`/`workflow_*` per that doc's own classification table) — specifically `run_code`, `send_email`, `github_tool`, `screenshot_url`, `hr_template_generate`, and the `vision_*` family — for each, either wire it correctly into `TOOL_RISK`/`SLUG_ALLOWED_RISKS` if it's meant to work (via the same Section-7-style staged approach: classify first, add a coverage test, only then widen `SLUG_ALLOWED_RISKS` in a reviewed commit) or add an explicit test proving it fails closed by design and document it as intentionally inert in the capability inventory. Before doing so, grep for a finance-contracts-style governing doc for any of these six names too — do not assume none exists just because this plan didn't find one.

## Phase E — Evidence and reporting

- [ ] Generate `docs/validation/coverage-report.json` and `docs/validation/known-limitations.json` summarizing final Genesis-side coverage against the full agent/capability/tool/failure matrix built in Phases B-D: fully tested / partially tested / untested agent counts, tested vs. total declared capabilities, disabled capabilities (from Phase A/D), and every remaining known gap with a reason — ready to feed the joint Part 7 independent-validation handoff once Cato's side is also GREEN.
